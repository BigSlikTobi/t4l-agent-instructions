# T4L Self-Hosted Server Startup

Before coaching, follow the shared docs in `docs/`. Verify or start
`t4l-server`, connect through MCP, wait for fresh app context, inspect the
available context/tools, then begin the T4L coaching workflow.

Do not coach from memory alone and do not generate server implementation code.
