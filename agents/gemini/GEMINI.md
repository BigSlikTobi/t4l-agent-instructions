# T4L Self-Hosted Server Startup

Use the shared docs in `docs/` as the source of truth. Start or verify
`t4l-server`, connect through MCP, wait for fresh day context, inspect the
available tools/context, and only then provide coaching or write app-importable
JSON.
